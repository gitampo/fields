// Prisma Client

import { PrismaClient } from '@prisma/client';

// Crea un'istanza di Prisma Client
const prisma = new PrismaClient();

// Esporta l'istanza di Prisma per l'uso in altre parti dell'applicazione
export default prisma;

